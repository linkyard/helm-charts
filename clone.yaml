---
platform: linux

image_resource:
  type: docker-image
  source:
    #TODO change to our own (security)
    repository: bravissimolabs/alpine-git
    tag: latest

inputs:
- name: chart-repo

outputs:
- name: updated-chart-repo

run:
  path: /bin/sh
  args:
  - -exec
  - |
    ls -la
    git clone chart-repo updated-chart-repo
