apiVersion: v1
kind: Secret
metadata:
  name: {{ template "fullname" . }}
  labels:
{{ include "labels" . | indent 4 }}
data:
  admin-user: {{ "admin" | b64enc | quote }}
  admin-password: {{ default (cat .Release.Name .Chart.Name | sha256sum) .Values.adminPassword  | b64enc | quote }}
