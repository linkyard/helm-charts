apiVersion: v1
kind: Secret
metadata:
  name: {{ template "fullname" . }}
  labels:
{{ include "labels" . | indent 4 }}
data:
  username: {{ "postgres" | b64enc | quote }}
  password: {{ default (cat .Release.Name .Chart.Name | sha256sum) .Values.password  | b64enc | quote }}
