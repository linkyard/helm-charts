linkyard Helm Charts
====================

Helm is a package manager for kubernetes: https://github.com/kubernetes/helm

This repository contains linkyards charts (packages) for helm.

To use execute:
  `helm repo add linkyard http://charts.linkyard.ch`
