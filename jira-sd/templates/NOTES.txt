JIRA Service Desk is now available at https://{{ template "hostname" . }}.

You may log in as administrator with:
  User:     {{ .Values.adminUser }}
  Password: {{ default (cat .Release.Name .Chart.Name | sha256sum) .Values.adminPassword }}
